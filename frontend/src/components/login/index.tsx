import React, { FC } from 'react';
import * as Styled from './style';

const {
  LoginSection,
  Box
} = Styled
export const Login:FC = () => {
  return(
    <LoginSection>
      <Box>
        Hello
      </Box>
    </LoginSection>
  ) 
}